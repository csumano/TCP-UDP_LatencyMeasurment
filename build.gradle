apply plugin: 'idea'
apply plugin: 'java'

sourceCompatibility='1.8'
targetCompatibility='1.8'

jar {
    manifest {
        attributes (
            'Class-Path': configurations.compile.collect { it.getName() }.join(' '),
            'Main-Class': 'com.csc445.assignment1.Main'
        )
    }
}